# GIT

```
ssk-keygen -t rsa
# Add to GitHUb
GIT_COMMITTER_NAME='TeraTree dev' GIT_COMMITTER_EMAIL='' git commit --author 'TeraTree dev <>' -m 'Experience templates, awscli, herokucli, migrations'
GIT_SSH_COMMAND="ssh -i ~/.ssh/teratree" git push -u origin master
```
