from django.urls import path

from . import views

app_name = 'experience'
urlpatterns = [
    # path('<int:experience_id>/', views.experience_detail, name='experience-detail'),
    # path('', views.experience_index, name='experience-index'),
    # path('create', views.experience_create, name='experience-create'),
]
